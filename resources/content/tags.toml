# Реестр тегов. Единственное место, где тег заводится.
#
# ЗАЧЕМ ПЕРЕЧЕНЬ, А НЕ СВОБОДНЫЕ СТРОКИ. Если тег появляется от одного лишь употребления,
# то опечатка заводит новый тег: написал "structrue" — получил сущность, которая ни в один
# отбор не попадает, и молча. С перечнем опечатка становится ошибкой при запуске.
#
# Порядок здесь — это порядок битов в маске. Переставлять можно свободно: тегами
# никто не пишет сохранений, снаружи нумерация не видна. Предел — 64 тега.
#
# Теги structure, mobile и enemy обязательны: на них опирается код.
# Тир (t1 / t2 / titan): если ни одного нет, ContentCompiler ставит t1 сам.

tags = [
	# Род сущности
	"structure",   # занимает клетки, стоит на месте
	"mobile",      # ходит по миру
	"commander",   # тот самый, единственный
	"enemy",       # сторона противника

	# Занятие
	"builder",     # умеет строить и чинить
	"defense",     # оборона: турели и всё, что стреляет с места
	"economy",     # производство и хранение ресурсов

	# Тир
	"t1",          # первая ступень (ставится по умолчанию, если тир не указан)
	"t2",          # вторая ступень
	"titan",       # одиночная тяжёлая машина: строится каркасом, приходит своей волной

	# Прочее
	"important",   # цель, потеря которой болезненна: база, реактор
]
