# Манипулятор башни-сборщика. Дальность равна её обзору: башня с места не сходит,
# и достаёт ровно до того, что видит.
# Числа — fab_tower celestial (metal 18 / energy 1000 → 10 на сжатой шкале).

id   = "assembler_arm"
name = "Манипулятор сборщика"
kind = "work"

range            = 9.0
power            = 18.0
energy_per_power = 0.556
works            = ["build"]
repairs_units    = true
